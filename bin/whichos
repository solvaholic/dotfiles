#!/bin/sh
#
# Try to find out which OS we're using, and print its name.
#
# Output: MacOS, Linux, OpenBSD, or idfkbro
#

case "$(uname)" in
  *Linux)
    MYOS='Linux'
    ;;
  Darwin)
    MYOS='MacOS'
    ;;
  OpenBSD)
    MYOS="OpenBSD"
    ;;
  *)
    MYOS="idfkbro"
    ;;
esac

echo "$MYOS"
